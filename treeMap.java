import java.util.*;

public class treeMap {
    public static void main(String args[]){
        Map<String, Integer> numbers = new TreeMap<>();

        // tree map is same as HashMap but it stores the key value pairs in sorted order

        // sorting is done based on its keys

        // time complexity of TreeMap is O(logn) because it adds the Map to a Binary tree under the hood


    }
    
}
